{% extends 'admin/admin_base_layout.html.twig' %}

{% block container %}
    {{ form_start(form) }}

    {{ form_row(form.question) }}
    {{ form_row(form.questionGerman) }}
    {{ form_row(form.answer) }}
    {{ form_row(form.answerGerman) }}

    <button type="submit" class="btn btn-primary">Save</button>

    <a href="{{ path('admin_faq_section_list') }}" class="btn btn-secondary">Cancel</a>
    {{ form_end(form) }}
{% endblock %}