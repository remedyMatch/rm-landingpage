{% extends 'admin/admin_base_layout.html.twig' %}

{% block container %}
    {{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.titleGerman) }}

    <button type="submit" class="btn btn-primary">Save</button>

    <a href="{{ path('admin_faq_list_sections') }}" class="btn btn-secondary">Cancel</a>
    {{ form_end(form) }}
{% endblock %}